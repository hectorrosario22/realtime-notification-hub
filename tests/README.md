# Tests

Automated tests should live under this directory.

Suggested initial projects:

- `tests/NotificationHub.Api.Tests`
- `tests/NotificationHub.Workers.Tests` (optional after worker logic exists)

Test strategy should cover:

- Request validation
- Domain and application behavior
- Integration paths for API and messaging workflows
